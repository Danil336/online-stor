const goods = document.querySelector('.goods')

 for(let i = 0; i < goods.children.length; i++){
    if(!(goods.children[i].classList.contains('hide') || goods.children[i].classList.contains('hide2') || goods.children[i].classList.contains('hide3') || goods.children[i].classList.contains('hide4') || goods.children[i].classList.contains('hide5'))){
        continue
    } else{
    }
 }